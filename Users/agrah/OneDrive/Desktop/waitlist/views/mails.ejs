<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Waitlist Subscribers</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #0d1117; color: #e6edf3; padding: 2rem; }
        .container { max-width: 800px; margin: auto; background-color: #161b22; border: 1px solid #30363d; border-radius: 12px; padding: 2rem; }
        h1 { border-bottom: 1px solid #30363d; padding-bottom: 1rem; margin-top: 0; }
        ul { list-style-type: none; padding: 0; }
        li { background-color: #0d1117; padding: 0.75rem 1rem; border-radius: 6px; margin-bottom: 0.5rem; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Waitlist Subscribers (<%= emails.length %>)</h1>
        <% if (emails.length > 0) { %>
            <ul>
                <% emails.forEach(email => { %>
                    <li><%= email %></li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No one has joined the waitlist yet.</p>
        <% } %>
    </div>
</body>
</html>